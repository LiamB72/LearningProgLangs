#include <stdio.h>


//not to be confused with integer, this one means "intiate"
int main(){

    printf("Hello World\nI'm happy to be alive");
//  Escape sequence = use a backslash (ctrl+8): \ + a character
//                    \n = new line
//                    \t = horizontal tab
//                    \v = vertical tab
//                    \\ = backslash
//                    \? = question mark
//                    \' = sigle quotation mark
//                    \" = double quotation mark

// "/* ... */" are used for multilines comments

    return 0;   //  if the code the code works fine, it input : code=0 
                //  else, if    have    an    error, it input : code=1 
}