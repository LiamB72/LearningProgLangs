#include <stdio.h>

int main(){

    // Format specifier always start by % = defines and formats a type of data to be displayed

    /*
    
    %c = Character
    %s = String
    %f = Float  (*)
    %lf= Double (*)
    %d = Integer

    (*) :
    %.n = decimal precision
    %n = how much space do you want before the value (includes the numbers of your value within the space count)
    %- = left align
    
    */
   
    return 0;
}